# =============================================================================
# DevRadar — Environment Variables Template
# =============================================================================
# Copie este arquivo para ".env" e preencha com seus valores reais.
# NUNCA faça commit do arquivo ".env".
#
#   cp .env.example .env
# =============================================================================

# --- SEGURANÇA ---
# Gere um valor seguro com: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=GERE_UMA_CHAVE_SECRETA_AQUI

# Chave de API para proteger endpoints de automação (Power Automate, etc.)
API_KEY=DEFINA_UMA_API_KEY_AQUI

# --- E-MAIL (Destinatários) ---
# E-mail(s) para receber os relatórios. Separe múltiplos com ponto-e-vírgula.
# Exemplo: usuario@seudominio.com;outrousuario@seudominio.com
EMAIL_DESTINATARIOS=SEU_EMAIL_AQUI

# E-mail(s) em cópia (CC). Deixe vazio se não houver.
EMAIL_COPIA=

# --- INTEGRAÇÃO (POWER AUTOMATE / WEBHOOK) ---
# URL do Flow responsável por processar os alertas.
# Deixe vazio se não usar integração via webhook.
POWER_AUTOMATE_WEBHOOK_URL=

# --- FUNCIONALIDADES (Dev vs Produção) ---
# Habilita o botão "Rodar Pipeline" no Dashboard.
# Em produção (Docker + Kestra), mantenha DESABILITADO (false ou ausente).
ENABLE_MANUAL_TRIGGER=false
